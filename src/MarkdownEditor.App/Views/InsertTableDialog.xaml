<Window x:Class="MarkdownEditor.App.Views.InsertTableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Insert Table"
        SizeToContent="Height" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="_Rows:" Target="{Binding ElementName=RowsBox}" VerticalAlignment="Center" />
        <TextBox x:Name="RowsBox" Grid.Row="0" Grid.Column="1" Margin="4" Padding="2" Text="3" />

        <Label Grid.Row="1" Grid.Column="0" Content="_Columns:" Target="{Binding ElementName=ColsBox}" VerticalAlignment="Center" />
        <TextBox x:Name="ColsBox" Grid.Row="1" Grid.Column="1" Margin="4" Padding="2" Text="3" />

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK" IsDefault="True" Click="Ok_Click" Width="80" Margin="0,0,8,0" />
            <Button Content="Cancel" IsCancel="True" Width="80" />
        </StackPanel>
    </Grid>
</Window>
