<Window x:Class="MarkdownEditor.App.Views.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="MarkStudio Editor Help"
        Icon="pack://application:,,,/Assets/app.ico"
        Height="720" Width="1080"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="220" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Background="#FFF7F7F7">
            <TextBox x:Name="SearchBox" DockPanel.Dock="Top" Margin="8"
                     Padding="4" VerticalContentAlignment="Center"
                     ToolTip="Search help topics"
                     TextChanged="SearchBox_TextChanged" />

            <TreeView x:Name="Tree" BorderThickness="0" Background="Transparent"
                      SelectedItemChanged="Tree_SelectedItemChanged">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="True" />
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Title}" Padding="0,2" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </DockPanel>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Background="#FFE0E0E0" ShowsPreview="False" />

        <wv2:WebView2 x:Name="ContentView" Grid.Column="2" DefaultBackgroundColor="White" />
    </Grid>
</Window>
