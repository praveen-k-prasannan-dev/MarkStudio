# Horizontal Rules

```
Above the line.

---

Below the line.
```

Above the line.

---

Below the line.

Any of `---`, `***`, or `___` on their own line produces the same rule. The ribbon's **Horizontal Rule** button ([Insert Tab — Blocks](../ribbon/insert-blocks.md)) always inserts `---`.
